You previously implemented a new competency model/system and initialized missing competencies. We do NOT want that. We already have an existing competency structure in our system and database.

NON-NEGOTIABLE

DO NOT create new competency models/collections/services.

DO NOT migrate or auto-fill challenges/users on startup.

DO NOT introduce defaults like “General”.

DO NOT change how competencies are stored (field names/shape) in the database.

Only implement:
(1) show challenge competency target
(2) update competency +1/-1 using the existing stored competency fields

1) Challenge competency target display (strict enum)

Use exactly these values:

Variables & Data Types

Control Structures

Functions

Arrays & Collections

Object-Oriented Programming

Error Handling

competencyTarget must be required for NEW/EDITED challenges.

For existing challenges missing it, show UI warning: “Missing competency target (admin must set)”. Do not default.

2) Competency update (use existing storage)
IMPORTANT

Before changing code:

Search the repo and identify the current existing competency storage, e.g. user.<something>.competency...

Keep the exact structure already used.

Implement only this logic at the current submit/grading endpoint:

Determine if user has a previous attempt for this challenge (using your existing progress/attempts tracking).

If current submission is correct → increase the competency bucket for the challenge’s competencyTarget by +1.

If current submission is wrong AND it’s a retake (previous attempt exists) → decrease that same competency bucket by -1 (floor at 0).

If current submission is wrong AND it is first attempt → do nothing (no decrease).

Do not add any other rules.

Missing key handling (very limited)

If the competency object exists but the specific competencyTarget key is missing, treat it as 0 and set only that key.

Do NOT create a new competency object if one already exists under another field; use the existing location.

OUTPUT REQUIRED

State exactly where competencies are stored (field path).

List the exact files changed.

Confirm you did not add new models, services, endpoints, or migrations.