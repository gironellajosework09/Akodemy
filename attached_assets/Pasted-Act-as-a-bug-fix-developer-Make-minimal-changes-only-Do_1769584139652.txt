Act as a bug-fix developer. Make minimal changes only. Do not redesign UI, models, or add new systems.

NON-NEGOTIABLE

DO NOT change badges.

DO NOT change grading logic.

DO NOT refactor the competency chart component design (it already exists).

DO NOT add migrations/services/new competency models.

Only fix data flow so competency charts update correctly.

BUG

Competency chart does not decrease when a student retakes a previously-correct challenge and submits incorrectly.
This affects:

Student Profile competency chart

Faculty > Student List > View Student > Competency chart (same chart)

Charts still behave like “once correct, always correct”.

REQUIRED FIXES
1) Backend: do NOT short-circuit on completed challenges

In the submission endpoint (progress/submit):

Remove/modify any early return like:

if (progress.completed) return res.json(...)


Keep completed true once solved (so checkmark remains), BUT still:

log the new attempt

apply competency updates

2) Always log every attempt (needed for retake detection)

Even if the challenge is already completed:

Store an attempt record (or increment attempts) in your existing progress structure.

Retake definition:

Retake = there exists any previous attempt for that challenge for that user.

3) Apply competency update rules (simple)

Using the existing stored competency fields (do not create new structure):

Correct attempt → +1 to that challenge’s competency target

Wrong attempt AND retake → -1 (floor at 0)

Wrong first attempt → no change

4) Charts must reflect latest data (student + faculty)

Since both student and faculty “view student” already have the chart:

Ensure both screens fetch competency values from the same updated source (same user field or same endpoint).

After submission, ensure the student profile chart refreshes (refetch user/profile data or update state).

Faculty view student must read the student’s latest saved competency values (no cached “completion %”).

ACCEPTANCE TESTS

Student solves a challenge correctly → student competency chart increases.

Student retakes same challenge and submits wrong → competency chart decreases.

Faculty view student competency chart shows the same decreased value.

Challenge remains checked/completed in list (✅ stays).

OUTPUT REQUIRED

Exact files changed

Where you removed the early return

Where attempts are logged on retakes

How the chart data is sourced/refreshed for both student and faculty views