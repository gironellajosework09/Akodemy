Act as a senior full-stack developer (React + Node.js/Express + MongoDB).
Implement an Admin account and update the login UI behavior as specified below.

1) Admin Account Setup (INITIAL SYSTEM ADMIN)
Create a default admin account with the following credentials:

Username: Admin

Password: Admin@2026

Role: admin

IMPORTANT SECURITY RULES:

The password MUST be hashed (bcrypt or equivalent) before saving to the database.

The plain password must never be:

Logged

Returned in API responses

Displayed in UI

This admin account should be created:

Either via a database seed script

Or via a one-time initialization check (create only if admin does not exist)

⚠️ Do NOT expose an admin registration form.

2) Role-Based Access Control (REQUIRED)

Ensure roles exist:

admin | student | faculty


Only users with role admin can:

Access /admin/* routes

Access /api/admin/* endpoints

Students and faculty must be blocked even if they manually enter admin URLs.

3) Login Page UI Changes (STRICT)
A) Remove Public Registration

REMOVE the “Register” option completely.

REMOVE:

Don't have an account? Register


There must be NO public self-registration for students or faculty.

B) Replace With Admin Login Option

Replace the register text with:

“Are you an admin? Login as admin”

Behavior:

Clicking “Login as admin” should:

Either redirect to an admin login page
OR

Switch the login form into admin mode (same form, different label)

Login Handling:

Admin login uses the same authentication system

After successful login:

Admin is redirected to Admin Dashboard

Students/faculty are redirected to their respective dashboards

4) Authentication Logic Rules

Authentication must:

Validate username/email + password

Determine role after login

Redirect based on role:

admin → Admin Dashboard

student → Student Dashboard

faculty → Faculty Dashboard

Admin users must not see student/faculty dashboards.

5) Acceptance Criteria (MUST PASS)

✅ Default admin account exists with username Admin
✅ Password Admin@2026 is hashed and secured
✅ No public registration option exists
✅ Login page shows “Are you an admin? Login as admin”
✅ Admin can log in successfully
✅ Admin is redirected to admin-only pages
✅ Students/faculty cannot access admin routes
✅ No password exposure anywhere in UI or API

6) Non-Negotiables

Do NOT add admin registration

Do NOT allow users to self-assign admin role

Do NOT store passwords in plain text

Do NOT change unrelated login or dashboard features