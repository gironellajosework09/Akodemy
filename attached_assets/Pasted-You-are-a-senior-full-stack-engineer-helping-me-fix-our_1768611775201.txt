You are a senior full-stack engineer helping me fix our autograder pipeline.

Goal

We run coding challenges with auto scoring. Our current issue: our platform’s test cases do not perfectly match Exercism test cases, causing inconsistent scoring. I need you to:

Ingest and normalize Exercism test cases for a specific exercise

Translate them into our platform’s test runner format (same assertions, same edge cases, same expected outputs)

Ensure our scoring logic is correct and deterministic

Cross-check the results stored in MongoDB Atlas to confirm the recorded score matches the computed score

What you must deliver
A) Test case alignment

Identify where our current tests diverge from Exercism:

missing edge cases

different expected outputs

input parsing differences

order of tests / skipped tests

locale/format issues (floats, rounding, whitespace)

Produce a canonical test spec that matches Exercism exactly.

Create a converter that transforms Exercism tests into our internal schema.

B) Autograder execution contract

Define and implement a strict contract:

Inputs: how user code is executed (function signature vs stdin/stdout)

Outputs: exact formatting rules

Errors: how runtime errors/timeouts are scored

Determinism: no randomness, fixed seeds if needed

Performance: time/memory limits and how they map to points

C) MongoDB Atlas cross-check

We store attempts and scores in MongoDB Atlas. Implement a verification step:

After grading, compute:

passedCount, failedCount, totalTests

score (per our scoring rubric)

per-test results (name/id, pass/fail, error message)

Compare computed values vs what is stored in Atlas for that submission:

If mismatch: log a “score_mismatch” event with details

Optionally: correct the DB record safely (only if we allow it)

Provide queries + indexes to make this fast.

Constraints / rules

Do NOT weaken tests to make more submissions pass.

The platform must reflect Exercism correctness. Only adjust OUR tests and runner to match Exercism spec.

Add clear logging and a debug mode to reproduce a failing scoring discrepancy.

Add unit tests for the converter and grading logic.

Inputs you should ask me for ONLY if truly required

If you need missing details, infer defaults and proceed, but prefer using these files if present:

exercism test definition source (commonly: canonical-data.json, or language-specific test file)

our current test file(s) or internal test schema

our grading service code (runner)

MongoDB collection names and attempt schema