Act as a senior full-stack developer (React + Node.js/Express + MongoDB).
Implement an Admin account/role whose responsibility is STRICTLY LIMITED to managing user accounts for students and faculty.

1) Scope (STRICT — DO NOT EXPAND)
Admin CAN:

✅ View user accounts (students and faculty)
✅ Add a single account
✅ Bulk add accounts via Excel template upload

Admin CANNOT:

❌ Access challenges, submissions, badges, grading, analytics, or coding playground
❌ View or retrieve passwords
❌ Modify student/faculty features beyond access control

2) Roles & Authorization

Ensure user roles exist:

admin | student | faculty


Protect all admin UI routes and APIs using server-side RBAC

Non-admin users must be blocked even if URLs are accessed directly

3) Admin UI – User Management Page
A) View Accounts (Users Table)
Column Order (MANDATORY — EXACT ORDER):

UID

Full Name

Email

Role

Year Level & Section (student only; “—” for faculty)

Created At (optional)

Full Name Display Rule (REQUIRED):

Full Name must be displayed as:

LastName, GivenName MiddleName


Middle Name:

Display only if present

Do NOT display extra spaces if empty

Example:

Dela Cruz, Juan Miguel
Santos, Ana

Pagination (REQUIRED)

Show 10 users per page

Use server-side pagination

Provide navigation controls (Prev / Next or page numbers)

Global Search (CRITICAL)

Search must be server-side

Must search across ALL records, not only the visible page

Searchable fields:

UID

Last Name

Given Name

Middle Name

Email

When searching:

Automatically reset to page 1

Pagination reflects only filtered results

B) Add Single Account
Required Fields:

UID

Last Name

Given Name

Middle Name

Email

Role (student / faculty)

Year Level & Section (REQUIRED if role = student)

UID Behavior (MANDATORY):

UID must always be:

First field

Unique

If role === student:

UID acts as Student ID

If role === faculty:

UID acts as Faculty ID

Validation Rules:

Email must be unique

UID must be unique

Required fields must not be empty

Confirmation modal before saving

Success feedback after creation

C) Bulk Add Accounts via Excel
1) Download Excel Template

Provide Download Excel Template button.

REQUIRED COLUMN ORDER (EXACT — UID MUST BE FIRST):
UID
LastName
GivenName
MiddleName
Email
YearLevelAndSection
Role


⚠️ Column order must be strictly enforced.

Template Rules:

Role must be exactly:

student | faculty


UID:

Required

Unique

Student ID if role = student

Faculty ID if role = faculty

YearLevelAndSection:

REQUIRED for students

Must be empty or ignored for faculty

2) Upload Excel File
Backend Validation (PER ROW):

UID uniqueness (file + database)

Email uniqueness and format

Required fields presence

Role value validation

YearLevelAndSection logic based on role

Insert Behavior (REQUIRED):

Insert valid rows

Skip invalid rows

Return detailed error report:

Row number

Column

Reason

Upload Result UI:

“Successfully added N users”

Show skipped rows with exact validation errors

Refresh table while maintaining search & pagination state

4) Backend API Requirements
List Users
GET /api/admin/users?page=1&limit=10&search=&role=all

Create Single User
POST /api/admin/users

Download Template
GET /api/admin/users/template

Bulk Upload
POST /api/admin/users/bulk


All endpoints must be admin-only.

5) Database Rules (MongoDB)

User schema must include:

uid (unique, indexed)

lastName

givenName

middleName

email (unique, indexed)

role

yearLevelAndSection (student only)

password (hashed only)

⚠️ Passwords must never be returned or displayed.

6) Acceptance Criteria (MUST PASS)

✅ UID is the first column in Excel and UI table
✅ Full Name is displayed as “LastName, GivenName MiddleName”
✅ UID behaves as Student ID or Faculty ID based on role
✅ Pagination shows 10 users per page
✅ Search works globally across all pages
✅ Bulk Excel upload works with validation and error reporting
✅ Non-admins cannot access admin routes
✅ No password exposure anywhere