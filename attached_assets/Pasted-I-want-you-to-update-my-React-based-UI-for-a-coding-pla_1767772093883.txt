I want you to update my React-based UI for a coding playground with a modal-first challenge entry experience.
The UI must be clear, student-friendly, and non-blocking, with instructions and coding always accessible.

2. Challenge Entry Modal (Required)

When a user clicks a challenge, DO NOT open the editor immediately.

First, display a modal dialog containing:

Modal Content

Best Time (fastest attempt time for this challenge)

Number of Runs (from latest attempt)

Latest Code Submission (read-only preview)

Modal Actions

Back to Challenges

Closes the modal

Returns to challenge selection screen

Start New Attempt

Closes the modal

Opens the coding editor for a fresh attempt

3. Starting a New Attempt

When the user clicks Start New Attempt:

Instructions are shown immediately by default

Editor and Run/Submit buttons are always visible

4. Instructions Panel

Instructions must:

Be visible immediately on challenge entry

Be step-by-step (not continuous text)

Be toggleable (Show / Hide)

Hiding instructions must not hide or resize the editor

5. View Latest Code Submission (During New Attempt)

While coding a new attempt, provide a button:

“View Latest Submission”

Clicking this button:

Opens a modal or side panel

Displays the latest code submission in read-only mode

Does NOT overwrite the current editor code

7. Submission Behavior

On submit:

Save final code

Save number of runs

Save time spent (bestTime)

Save as a new attempt in history

Show a success message after submission

8. History Panel

Display previous attempts with:

Attempt number

Submission date & time

Runs

Time spent

Score / correctness

Include Attempt Again button

9. UX / UI Rules (Important)

Modal is mandatory before starting a challenge

Instructions are visible immediately

Editor is never blocked

Latest submission is always read-only

No auto-loading of previous code into editor
