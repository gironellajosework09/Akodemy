Task 1 — Fix Result Modal “Next” Flow (Show Instructions First)

Currently:

Retry shows the Instructions view first (correct behavior).

Next goes directly to the Coding Playground (incorrect behavior).

Update behavior so that:

Clicking Next must NOT open the coding playground immediately.

Clicking Next must first navigate to or render the Instructions screen for the next challenge (same flow as Retry).

Only after the user clicks Start / Begin / Continue on the Instructions screen should the Coding Playground open.

State management must reset cleanly:

Clear previous logs/output/test results

Load next challenge data reliably

Avoid race conditions and double navigation (disable Next while navigating)

Support:

Next challenge in same module

Next section/difficulty if applicable

Implementation requirement:
Refactor navigation so both Retry and Next go through a shared function like goToInstructions(challengeId) and only after that startChallenge(challengeId) opens the playground.

Task 2 — Add Smooth & Seamless Transitions Everywhere

Implement consistent, subtle transitions across all view/page navigation, including:

Result Modal → Instructions

Instructions → Coding Playground

Challenge list → Instructions

Profile → Badges/Titles

Sidebar navigation route changes

Requirements:

Use fast, subtle animations (fade/slide) 150–300ms

No flicker, no layout jump, no “double render” feeling

Reset or preserve scroll intentionally per screen

Create a reusable transition wrapper so everything feels consistent

Accessibility:

Respect prefers-reduced-motion (reduce/disable animations)

Maintain keyboard focus (focus should move meaningfully after route/view change)

Provide code-level guidance:

If React: recommend a practical approach (Framer Motion / React Transition Group / CSS transitions)

If plain JS: provide a reusable transition helper/wrapper pattern

Task 3 — Fix Sign-In Page: Show Error Without Clearing Inputs

Currently:

If email/password is wrong, it shows error (or attempts to) BUT it erases user input completely.

Update behavior so that:

On wrong email/password:

Show a clear error message (e.g., “Invalid email or password.”)

Do NOT clear the email input

Do NOT clear the password input automatically

Only clear inputs when:

The user explicitly clicks a “Clear” button (optional)

Or on successful login (password can be cleared on success)

UX polish:

Highlight invalid form state (red border, etc.)

Keep cursor focus:

If email format invalid → focus email field

If credentials invalid → focus password field (optional, but do not erase it)

Ensure no sensitive leakage:

Do not show whether email exists; use generic error for security

Handle request states:

Disable Sign In button while submitting

Prevent multiple submissions

Show loading indicator

Implementation hint:

Keep input values controlled by state (email, password)

On error response, set errorMessage only—do not reset input state

Only reset state on successful login